# Archive

Legacy scripts and documents that are **not used by the current pipeline**. Kept for reference only.

- **Scripts**: Old digest generators, HTML generators, and one-off utilities (e.g. `ethical_news_digest.py`, `generate_html.py`, `create_language_template.py`) — superseded by the `digest/` package and `scripts/github_ai_news_digest.py`.
- **Docs**: Historical planning (e.g. `regional_localization_plan.md`, `regional_implementation_strategy.md`).
- **Reports**: Old Lighthouse and test outputs — safe to delete if you need space.

Do not rely on these for current development. See project root `README.md` and `docs/PROJECT_STRUCTURE.md` for up-to-date structure.
